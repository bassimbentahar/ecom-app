eureka:
  client:
    service-url:
      defaultZone: http://discovery-api:8761/eureka

spring:
  cloud:
    config:
      override-system-properties: false

management:
  tracing:
    sampling:
      probability: 1.0
